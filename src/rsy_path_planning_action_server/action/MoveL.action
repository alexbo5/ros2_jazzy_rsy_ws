# MoveL Action - Linear (Cartesian) motion
# Moves the robot TCP linearly to target pose (straight line in Cartesian space)

# Goal
string robot_name                    # "robot1" or "robot2"
float64[16] transformation_matrix    # 4x4 transformation matrix (row-major order)
                                     # [r00, r01, r02, tx, r10, r11, r12, ty, r20, r21, r22, tz, 0, 0, 0, 1]
string frame_id                      # Reference frame for the transformation (e.g., "world", "robot1_base_link")
float64 velocity_scaling             # Velocity scaling factor (0.0 to 1.0), default 0.1
float64 acceleration_scaling         # Acceleration scaling factor (0.0 to 1.0), default 0.1
---
# Result
bool success                         # True if motion completed successfully
string message                       # Status message
---
# Feedback
float64 progress                     # Progress percentage (0.0 to 100.0)
float64[16] current_transformation   # Current end effector transformation matrix
