# MoveJ Action - Point-to-Point (PTP) motion via Pilz planner
# Moves the robot to target Cartesian pose using joint space interpolation

# Goal
string robot_name       # "robot1" or "robot2"
float64 x               # Target X position [m]
float64 y               # Target Y position [m]
float64 z               # Target Z position [m]
float64 qx              # Target orientation quaternion X
float64 qy              # Target orientation quaternion Y
float64 qz              # Target orientation quaternion Z
float64 qw              # Target orientation quaternion W
string frame_id         # Reference frame (default: "world")
---
# Result
bool success            # True if motion completed successfully
string message          # Status message
---
# Feedback
string status           # Current execution status
